{
  "name": "@weekday/db",
  "version": "0.0.0",
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "prebuild": "bun with:env prisma generate --no-engine",
    "postinstall": "bun with:env prisma generate",
    "generate": "bun with:env prisma generate",
    "migrate": "bun with:env prisma migrate dev",
    "deploy": "bun with:env prisma migrate deploy",
    "reset": "bun with:env prisma migrate reset",
    "push": "bun with:env prisma db push",
    "studio": "bun with:env prisma studio",
    "with:env": "dotenv -e ../../.env --"
  },
  "dependencies": {
    "@prisma/client": "^6.5.0",
    "@weekday/env": "*"
  },
  "devDependencies": {
    "prisma": "^6.5.0",
    "typescript": "^5.8.2"
  }
}
