{
  "name": "@weekday/db",
  "version": "0.0.0",
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "generate": "bun run with:env drizzle-kit generate",
    "migrate": "bun run with:env drizzle-kit migrate",
    "push": "bun run with:env drizzle-kit push",
    "studio": "bun run with:env drizzle-kit studio",
    "with:env": "dotenv -e ../../.env --"
  },
  "dependencies": {
    "@weekday/env": "*",
    "drizzle-orm": "^0.43.1",
    "pg": "^8.16.0"
  },
  "devDependencies": {
    "@types/pg": "^8.15.2",
    "drizzle-kit": "^0.31.1",
    "typescript": "^5.8.2"
  }
}
